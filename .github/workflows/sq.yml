- name: SonarQube Scan
  run: |
    pytest --cov=. --cov-report=xml
    sonar-scanner \
      -Dsonar.projectKey=voter-system \
      -Dsonar.sources=. \
      -Dsonar.host.url=http://localhost:9000 \
      -Dsonar.login=${{ secrets.SONAR_TOKEN }}
